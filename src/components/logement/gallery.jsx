import React from "react";

export default function CompGallery(gal) {
    return(
        <div>
            {/* insérer ici le carrousel */}
            {gal}
            <span>
                {/* insérer ici le numéro de l'image / le nombre d'image */}
            </span>
        </div>
    ); 
}